{
  "permissions": {
    "allow": [
      "Read(./**)",
      "Edit(./**)",
      "Bash(pnpm test:*)",
      "Bash(pnpm lint:*)",
      "Bash(pnpm typecheck:*)",
      "Bash(pnpm exec vitest:*)",
      "Bash(git status)",
      "Bash(git diff:*)",
      "Bash(git log:*)"
    ],
    "deny": ["Read(.env*)"]
  },
  "$comment": [
    "Interactive sessions only. `Edit(./**)` covers every file-editing tool, Write included;",
    "a `Write(...)` rule is ignored and warned about at startup. The headless profile the AI",
    "layer spawns the CLI under lives in .claude/run-settings.json (passed with --settings)",
    "so it cannot widen this session's permissions — see docs/04-ai-layer.md."
  ]
}
